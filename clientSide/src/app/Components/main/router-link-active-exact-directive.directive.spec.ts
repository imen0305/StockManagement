import { RouterLinkActiveExactDirectiveDirective } from './router-link-active-exact-directive.directive';

describe('RouterLinkActiveExactDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterLinkActiveExactDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
